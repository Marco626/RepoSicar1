# Iniciar un repositorio: git init

# Consultar estado de un repositorio: git status

# Nuevos archivos sin guardar: git status

# Agregar archivos nuevos: git add <nombre>

# Consultar commits realizados: git log

# Configurar datos: 
	Nombre > git config --global user.name "John Doe"
	Correo > git config --global user.email johndoe@example.com

# Guardar cambios: git commit -a -m "Comentario"  (-am)(concatenar abreviaturas)


# Ctrl + Shift + P : Paquetes Sublime, (Sublimer) Comparar Revision